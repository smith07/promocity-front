import { PutForwardComponentDirective } from './put-forward-component.directive';

describe('PutForwardComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new PutForwardComponentDirective();
    expect(directive).toBeTruthy();
  });
});
